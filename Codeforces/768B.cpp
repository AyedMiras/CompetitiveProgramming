#include <bits/stdc++.h>
#define ll long long int
using namespace std;
ll n,l,r,ind,res;
map<pair<ll,ll> > m;

int main() {
    ios::sync_with_stdio(0);
    cin >> n >> l >> r;
    cout << dc(n) << endl;
}
